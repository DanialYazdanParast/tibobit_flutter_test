import 'package:flutter/material.dart';

class AppConstants {
  // Animation
  static const Duration animationDuration = Duration(milliseconds: 300);

  // Card Heights
  static const double cardBaseHeight = 130.0;
  static const double expandedExtraHeight = 80.0;
  static const double normalCardHeight = 90.0;

  // Colors
  static const Color levelOneCardColor = Color(0xFF242633);
  static const Color selectedCardColor = Color(0xFF1F1F1F);
  static const Color defaultCardColor = Color(0xFF181818);
  static const Color verificationButtonColor = Color(0xff31A300);

  // Spacing
  static const double cardHorizontalPadding = 16.0;
  static const double cardVerticalPadding = 16.0;
  static const double cardBorderRadius = 20.0;
}
